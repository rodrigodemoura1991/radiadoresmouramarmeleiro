(function(){'use strict';const $=id=>document.getElementById(id);let clientIndex=-1;function buttons(){const b=$('clientSug');return b?[...b.querySelectorAll('button[data-id]')]:[]}function paint(){const a=buttons();a.forEach((b,i)=>b.classList.toggle('keyboard-active',i===clientIndex));if(a[clientIndex])a[clientIndex].scrollIntoView({block:'nearest'})}function choose(){const a=buttons();if(clientIndex<0||!a[clientIndex])return false;a[clientIndex].click();clientIndex=-1;return true}function cycle(el,d){const n=el.options.length;el.selectedIndex=(el.selectedIndex+d+n)%n;el.dispatchEvent(new Event('change',{bubbles:true}));el.dispatchEvent(new Event('input',{bubbles:true}))}function bind(){const c=$('clientInput');if(c&&!c.dataset.kn){c.dataset.kn='1';c.addEventListener('input',()=>clientIndex=-1);c.addEventListener('keydown',e=>{if(!$('clientSug')||$('clientSug').classList.contains('hidden'))return;const a=buttons();if(e.key==='ArrowDown'){e.preventDefault();e.stopPropagation();if(a.length){clientIndex=Math.min(clientIndex+1,a.length-1);paint()}}else if(e.key==='ArrowUp'){e.preventDefault();e.stopPropagation();if(a.length){clientIndex=clientIndex<=0?0:clientIndex-1;paint()}}else if(e.key==='Enter'){if(choose()){e.preventDefault();e.stopPropagation()}}else if(e.key==='Escape'){clientIndex=-1;$('clientSug').classList.add('hidden');e.preventDefault();e.stopPropagation()}},true)}document.addEventListener('keydown',e=>{const el=e.target;if(!el||el.tagName!=='SELECT')return;const p=el.id==='payment'||el.id==='fixPayment',s=el.classList.contains('status')||el.classList.contains('fst');if(!p&&!s)return;if(e.key==='ArrowDown'||e.key==='ArrowUp'){e.preventDefault();e.stopPropagation();cycle(el,e.key==='ArrowDown'?1:-1)}else if(e.key==='Enter'){e.preventDefault();e.stopPropagation();el.blur()}},true)}
function bindClearLaunchFilters(){const filters=$('launchFilters');if(!filters||$('clearLaunchFilters'))return;const btn=document.createElement('button');btn.type='button';btn.id='clearLaunchFilters';btn.className='btn';btn.textContent='✕ Limpar filtros';btn.title='Limpar pesquisa e filtros de lançamentos';btn.style.cssText='white-space:nowrap;cursor:pointer;';btn.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();const search=$('launchSearch'),payment=$('launchPaymentFilter'),status=$('launchStatusFilter');if(search)search.value='';if(payment)payment.value='';if(status)status.value='';if(typeof window.renderLaunches==='function')window.renderLaunches();});filters.appendChild(btn)}
function bindLaunchCardKeyboard(){const list=$('launchList');if(!list||list.dataset.cardKeyboard==='1')return;list.dataset.cardKeyboard='1';document.addEventListener('keydown',e=>{if(e.key!=='ArrowDown'&&e.key!=='ArrowUp')return;const target=e.target;if(target&&(target.tagName==='INPUT'||target.tagName==='TEXTAREA'||target.tagName==='SELECT'||target.isContentEditable))return;const cards=[...list.querySelectorAll('.launch-reference-card')];if(!cards.length)return;const selected=list.querySelector('.launch-reference-card.launch-card-selected');let index=selected?cards.indexOf(selected):-1;if(e.key==='ArrowDown')index=index<0?0:Math.min(index+1,cards.length-1);else index=index<0?cards.length-1:Math.max(index-1,0);const card=cards[index];if(!card)return;e.preventDefault();e.stopPropagation();card.click();card.scrollIntoView({block:'nearest',behavior:'smooth'});},true)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{bind();bindClearLaunchFilters();bindLaunchCardKeyboard()},{once:true});else{bind();bindClearLaunchFilters();bindLaunchCardKeyboard()}setTimeout(bindClearLaunchFilters,300);setTimeout(bindLaunchCardKeyboard,500);const st=document.createElement('style');st.textContent='.suggest button.keyboard-active{background:#e8f0ff!important;outline:2px solid rgba(30,80,160,.18)}';document.head.appendChild(st)})();